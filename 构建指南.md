# QuickGemini - GitHub Actions 构建指南

## 步骤 1：创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称填 `QuickGemini`
3. 选择 **Public** 或 **Private** 都可以
4. **不要**勾选 "Initialize this repository with a README"
5. 点击 "Create repository"

## 步骤 2：上传代码到 GitHub

在电脑上打开命令行（PowerShell 或 CMD），进入 `D:\Downloads\codepilot\QuickGemini` 目录：

```powershell
cd D:\Downloads\codepilot\QuickGemini
```

然后依次执行：

```powershell
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/QuickGemini.git
git push -u origin main
```

**注意**：把 `你的用户名` 换成你实际的 GitHub 用户名。

## 步骤 3：触发构建

1. 打开你的 GitHub 仓库页面
2. 点击顶部的 **Actions** 标签
3. 在左侧选择 **Build APK**
4. 点击右侧的 **Run workflow** → 绿色的 **Run workflow** 按钮
5. 等待约 2-3 分钟，构建完成后会出现绿色勾号

## 步骤 4：下载 APK

1. 点击那个绿色勾号的工作流
2. 滚动到页面底部的 **Artifacts** 区域
3. 点击 **quickgemini-debug-apk** 下载
4. 解压下载的 zip 文件，里面就是 `app-debug.apk`

## 步骤 5：安装到手机

1. 把 `app-debug.apk` 传到手机
2. 在手机上点击安装
3. 如果提示"未知来源"，允许安装即可

## 使用说明

1. 打开手机设置，找到类似"默认应用"或"语音助手"的选项
2. 把默认语音助手设为 **Gemini**
3. 点击 QuickGemini 图标，即可唤起 Gemini 浮层

---

## 常见问题

**Q: 构建失败怎么办？**
A: 检查 Actions 日志，通常是 Gradle 依赖下载问题，重试一次即可。

**Q: 可以构建 Release 版本吗？**
A: 当前工作流只构建 Debug 版本，足够日常使用。Release 版本需要签名，比较麻烦。
